file(GLOB DISCO_FRAMEWORK_SOURCES *.cpp)
file(GLOB DISCO_FRAMEWORK_HEADERS *.h)

add_library(${LIBRARY_NAME}framework
    ${DISCO_FRAMEWORK_SOURCES}
    ${DISCO_FRAMEWORK_HEADERS})

install(TARGETS ${LIBRARY_NAME}framework DESTINATION lib)
