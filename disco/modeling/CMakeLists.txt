file(GLOB DISCO_MODELING_SOURCES *.cpp)
file(GLOB DISCO_MODELING_HEADERS *.h)

add_library(${CMAKE_PROJECT_NAME}modeling SHARED
    ${DISCO_MODELING_SOURCES}
    ${DISCO_MODELING_HEADERS})

install(
    TARGETS ${CMAKE_PROJECT_NAME}modeling
    DESTINATION ${DISCO_SHARED_LIBRARY_DESTINATION}
    )
